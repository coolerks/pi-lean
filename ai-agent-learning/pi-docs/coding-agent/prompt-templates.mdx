---
title: "提示词模板"
description: "创建可复用的 Markdown 提示词模板，并使用参数展开为完整提示词。"
---

> Pi 可以创建提示词模板。你可以让它为自己的工作流生成一个模板。

# 提示词模板

提示词模板是一种 Markdown 片段，使用时会展开为完整提示词。在编辑器中输入 `/name` 可以调用模板，其中 `name` 是去掉 `.md` 后的文件名。

## 存放位置

Pi 会从以下位置加载提示词模板：

- 全局：`~/.pi/agent/prompts/*.md`
- 项目：`.pi/prompts/*.md`（仅在项目获得信任后加载）
- 软件包：软件包的 `prompts/` 目录，或 `package.json` 中 `pi.prompts` 指定的位置
- 设置：`prompts` 数组中的文件或目录
- CLI：`--prompt-template <path>`（可以重复传入）

使用 `--no-prompt-templates` 可以关闭自动发现。

## 文件格式

```markdown
---
description: Review staged git changes
---
Review the staged changes (`git diff --cached`). Focus on:
- Bugs and logic errors
- Security issues
- Error handling gaps
```

文件名会变成命令名：`review.md` 对应 `/review`。`description` 是可选的；如果没有提供，Pi 会使用文件中的第一条非空行。`argument-hint` 也是可选的；如果配置，它会在自动补全菜单中显示在 description 前面。

### 参数提示

在 frontmatter 中使用 `argument-hint`，为自动补全菜单说明调用者应传入什么参数。尖括号表示必填参数，方括号表示可选参数：

```markdown
---
description: Review PRs from URLs with structured issue and code analysis
argument-hint: "<PR-URL>"
---
```

它会在自动补全菜单中显示为：

```text
→ pr   <PR-URL>       — Review PRs from URLs with structured issue and code analysis
  is   <issue>        — Analyze GitHub issues (bugs or feature requests)
  wr   [instructions] — Finish the current task end-to-end
  cl   — Audit changelog entries before release
```

`argument-hint` 只影响提示，不会替模板执行参数校验。模板正文中的占位符仍由展开规则处理。

## 使用方式

在编辑器中输入 `/`，再输入模板名称。自动补全菜单会显示可用模板和描述。

```text
/review                           # Expands review.md
/component Button                 # Expands with argument
/component Button "onClick handler" # Multiple arguments
```

调用时，模板文件本身会展开到提示词中；文件名是命令名，参数会根据下节规则替换。

## 参数

模板支持位置参数、默认值和简单切片：

- `$1`、`$2`、…：第 1、2 个位置参数。
- `$@` 或 `$ARGUMENTS`：所有参数拼接后的字符串。
- `${1:-default}`：如果第 1 个参数存在且非空则使用它，否则使用 `default`。
- `${@:-default}` 或 `${ARGUMENTS:-default}`：如果全部参数存在且非空则使用它，否则使用 `default`。
- `${@:N}`：从第 N 个参数开始取（参数位置从 1 开始）。
- `${@:N:L}`：从第 N 个参数开始取 L 个参数。

示例：

```markdown
---
description: Create a component
---
Create a React component named $1 with features: $@
```

默认值适合可选参数：

```markdown
Summarize the current state in ${1:-7} bullet points.
```

调用方式：`/component Button "onClick handler" "disabled support"`

上面的调用会把 `Button` 作为 `$1`，把其余参数组合后放入 `$@`。如果参数为空，带 `:-default` 的表达式会使用默认文本。

## 加载规则

- `prompts/` 中的模板发现不会递归进行。
- 如果模板位于子目录中，需要通过 settings 或 package manifest 明确添加它。

> 译者注：提示词模板只负责组装用户输入，不会因此获得额外的工具权限。模板可来自项目或软件包，所以加载前仍要考虑项目可信度；模板正文也可能影响模型行为，应像代码和上下文文件一样审查来源。

## 完整原文对照

[在 GitHub 打开源文件](https://github.com/coolerks/pi-lean/blob/853a80d26c90a14c1886f0ebb8ffaae133ca2185/packages/coding-agent/docs/prompt-templates.md)

````markdown
> pi can create prompt templates. Ask it to build one for your workflow.

# Prompt Templates

Prompt templates are Markdown snippets that expand into full prompts. Type `/name` in the editor to invoke a template, where `name` is the filename without `.md`.

## Locations

Pi loads prompt templates from:

- Global: `~/.pi/agent/prompts/*.md`
- Project: `.pi/prompts/*.md` (only after the project is trusted)
- Packages: `prompts/` directories or `pi.prompts` entries in `package.json`
- Settings: `prompts` array with files or directories
- CLI: `--prompt-template <path>` (repeatable)

Disable discovery with `--no-prompt-templates`.

## Format

```markdown
---
description: Review staged git changes
---
Review the staged changes (`git diff --cached`). Focus on:
- Bugs and logic errors
- Security issues
- Error handling gaps
```

- The filename becomes the command name. `review.md` becomes `/review`.
- `description` is optional. If missing, the first non-empty line is used.
- `argument-hint` is optional. When set, the hint is displayed before the description in the autocomplete dropdown.

### Argument Hints

Use `argument-hint` in frontmatter to show expected arguments in autocomplete. Use `<angle brackets>` for required arguments and `[square brackets]` for optional ones:

```markdown
---
description: Review PRs from URLs with structured issue and code analysis
argument-hint: "<PR-URL>"
---
```

This renders in the autocomplete dropdown as:

```
→ pr   <PR-URL>       — Review PRs from URLs with structured issue and code analysis
  is   <issue>        — Analyze GitHub issues (bugs or feature requests)
  wr   [instructions] — Finish the current task end-to-end
  cl   — Audit changelog entries before release
```

## Usage

Type `/` followed by the template name in the editor. Autocomplete shows available templates with descriptions.

```
/review                           # Expands review.md
/component Button                 # Expands with argument
/component Button "click handler" # Multiple arguments
```

## Arguments

Templates support positional arguments, defaults, and simple slicing:

- `$1`, `$2`, ... positional args
- `$@` or `$ARGUMENTS` for all args joined
- `${1:-default}` uses arg 1 when present/non-empty, otherwise `default`
- `${@:-default}` or `${ARGUMENTS:-default}` uses all arguments when present/non-empty, otherwise `default`
- `${@:N}` for args from the Nth position (1-indexed)
- `${@:N:L}` for `L` args starting at N

Example:

```markdown
---
description: Create a component
---
Create a React component named $1 with features: $@
```

Default values are useful for optional arguments:

```markdown
Summarize the current state in ${1:-7} bullet points.
```

Usage: `/component Button "onClick handler" "disabled support"`

## Loading Rules

- Template discovery in `prompts/` is non-recursive.
- If you want templates in subdirectories, add them explicitly via `prompts` settings or a package manifest.

````
