---
title: "容器化"
description: "使用 Gondolin、Docker 或 OpenShell 隔离 Pi 的运行环境和工具执行。"
---

# 容器化

> 本页根据 `packages/coding-agent/docs/containerization.md` 重新翻译，研究基线为 commit `853a80d26c90a14c1886f0ebb8ffaae133ca2185`。命令、路径、包名和协议名称保持原样。

## 中文译文

Pi 默认拥有启动它的进程所拥有的权限。但在需要限制可写目录或其他访问能力时，可以选择两种总体方案：

1. 把整个 `pi` 进程放入隔离环境；
2. 让 `pi` 继续运行在宿主机上，只把工具执行路由到隔离环境。

## 选择方案

| 方案 | 隔离对象 | 适用场景 | 注意事项 |
| --- | --- | --- | --- |
| Gondolin 扩展（Gondolin extension） | 内置工具和 `!` 命令 | 认证留在宿主机、本地工具在 micro-VM 中运行 | 参见 [`examples/extensions/gondolin/`](https://github.com/coolerks/pi-lean/tree/853a80d26c90a14c1886f0ebb8ffaae133ca2185/packages/coding-agent/examples/extensions/gondolin/)。 |
| 直接使用 Docker（Plain Docker） | 本地容器中的整个 `pi` 进程 | 简单的本地隔离 | Provider API key 会进入容器。 |
| OpenShell | 由策略控制的 sandbox 中的整个 `pi` 进程 | 本地或远程的受管隔离环境 | 需要 OpenShell gateway。 |

扩展（Extension）在 `pi` 进程所在的环境中运行。如果宿主机上的 Pi 使用了负责路由工具的扩展，其他自定义扩展工具仍然在宿主机执行，除非它们也主动把操作委托出去。

## Gondolin

[Gondolin](https://github.com/earendil-works/gondolin) 是本地 Linux micro-VM。需要让 Pi 继续在宿主机处理认证和界面、同时把内置工具路由到 VM 时，可以使用[示例扩展（Extension）](https://github.com/coolerks/pi-lean/tree/853a80d26c90a14c1886f0ebb8ffaae133ca2185/packages/coding-agent/examples/extensions/gondolin)。

安装：

```bash
cp -R packages/coding-agent/examples/extensions/gondolin ~/.pi/agent/extensions/gondolin
cd ~/.pi/agent/extensions/gondolin
npm install --ignore-scripts
```

在要挂载的项目目录中启动：

```bash
cd /path/to/project
pi -e ~/.pi/agent/extensions/gondolin
```

这个 Extension 会把宿主机的当前工作目录挂载到 VM 的 `/workspace`，并覆盖 `read`、`write`、`edit`、`bash`、`grep`、`find` 和 `ls`。用户输入的 `!` 命令也会路由到 VM。`/workspace` 下的文件修改会直接写回宿主机。

运行 `@earendil-works/gondolin` 需要 Node.js >= 23.6.0，还需要通过包管理器安装 QEMU。这里隔离的是被路由的操作；没有交给该 Extension 的工具，以及其他 Extension 的代码，仍在宿主机中运行。

## Plain Docker

如果希望获得最简单的本地容器边界，可以把整个 `pi` 进程放进 Docker。

`Dockerfile.pi`：

```dockerfile
FROM node:24-bookworm-slim

RUN apt-get update \
  && apt-get install -y --no-install-recommends bash ca-certificates git ripgrep \
  && rm -rf /var/lib/apt/lists/*
RUN npm install -g --ignore-scripts @earendil-works/pi-coding-agent

WORKDIR /workspace
ENTRYPOINT ["pi"]
```

构建并运行：

```bash
docker build -t pi-sandbox -f Dockerfile.pi .

docker run --rm -it \
  -e ANTHROPIC_API_KEY \
  -v "$PWD:/workspace" \
  -v pi-agent-home:/root/.pi/agent \
  pi-sandbox
```

`-v "$PWD:/workspace"` 把当前目录挂载到容器的 `/workspace`，所以容器内的读写会直接影响宿主文件。使用命名卷保存 `/root/.pi/agent` 时，设置和会话留在容器一侧；如果改为挂载宿主机的 `~/.pi/agent`，容器也会得到宿主机的认证和会话文件。

这不是凭据隔离：`-e ANTHROPIC_API_KEY` 明确将变量传进容器，项目目录挂载也使写入可以回到宿主机。不要把密钥放入 Dockerfile、镜像层或命令历史。

## OpenShell

需要同时控制文件系统、进程、网络、凭据和推理时，可以使用 [NVIDIA OpenShell](https://docs.nvidia.com/openshell/about/overview)。它可以通过 Docker、Podman 或 VM 运行时连接本地 gateway，也可以连接远程 Kubernetes gateway。

每个 sandbox 都需要活动 gateway。创建 sandbox 前先注册并选中 gateway：

```bash
openshell gateway add <gateway-url> --name <name>
openshell gateway select <name>
```

在 OpenShell sandbox 中启动 Pi：

```bash
openshell sandbox create --name pi-sandbox --from pi -- pi
```

这种模式会把整个 `pi` 进程放进 sandbox，因此内置工具、`!` 命令和扩展工具都在 OpenShell 边界内执行。

如果 gateway 在远端，项目文件不会自动从宿主机 bind mount 进去。可以在 sandbox 中 clone，或使用 OpenShell 的文件传输命令：

```bash
openshell sandbox upload pi-sandbox ./repo /workspace
openshell sandbox download pi-sandbox /workspace/repo ./repo-out
```

OpenShell provider 可以把原始模型 API key 留在 sandbox 外部。配置 inference routing 后，sandbox 中的代码可以访问 `https://inference.local`，由 gateway 向上游注入 provider credentials。若要使用这条路径，需要把 Pi 配置为对应的 OpenAI-compatible 或 Anthropic-compatible endpoint。

> 译者注：隔离“Pi 进程”、隔离“工具执行”和隔离“凭据”是三个不同问题。挂载项目目录会影响文件边界，gateway 路由会影响网络和认证边界；选择容器方案时应分别验证它们，而不是只看进程是否位于容器中。

## 完整原文对照

[在 GitHub 打开源文件](https://github.com/coolerks/pi-lean/blob/853a80d26c90a14c1886f0ebb8ffaae133ca2185/packages/coding-agent/docs/containerization.md)

````markdown
# Containerization

Pi runs with all permissions by default, but in some cases, you will want to have more control over what directories Pi can write to and which accesses it has.

There are two general options. You can either
1. run the whole `pi` process inside an isolated environment, or
2. run `pi` on the host and route tool execution into an isolated environment.

## Choose a pattern

| Pattern | What is isolated | Best for | Notes |
| --- | --- | --- | --- |
| Gondolin extension | Built-in tools and `!` commands | Local micro-VM isolation while keeping auth on host | See [`examples/extensions/gondolin/`](../examples/extensions/gondolin/). |
| Plain Docker | Whole `pi` process in a local container | Simple local isolation | Provider API keys enter the container. |
| OpenShell | Whole `pi` process in a policy-controlled sandbox | Local or remote managed sandbox | Requires an OpenShell gateway |

Extensions run wherever the `pi` process runs. If you run host `pi` with a tool-routing extension, other custom extension tools still run on the host unless they also delegate their operations.

## Gondolin

[Gondolin](https://github.com/earendil-works/gondolin) is a local Linux micro-VM.
Use the [example extension](../examples/extensions/gondolin) when you want `pi` on the host but all built-in tools routed into the VM.

Setup:

```bash
cp -R packages/coding-agent/examples/extensions/gondolin ~/.pi/agent/extensions/gondolin
cd ~/.pi/agent/extensions/gondolin
npm install --ignore-scripts
```

Run from the project you want mounted:

```bash
cd /path/to/project
pi -e ~/.pi/agent/extensions/gondolin
```

The extension mounts the host cwd at `/workspace` in the VM and overrides `read`, `write`, `edit`, `bash`, `grep`, `find`, and `ls`.
User `!` commands are routed into the VM, as well.
File changes under `/workspace` write through to the host.

Requirements: Node.js >= 23.6.0 for `@earendil-works/gondolin`, plus QEMU (requires installation through your package manager).

## Plain Docker

Run the whole `pi` process in Docker when you want the simplest local container boundary.

`Dockerfile.pi`:

```dockerfile
FROM node:24-bookworm-slim

RUN apt-get update \
  && apt-get install -y --no-install-recommends bash ca-certificates git ripgrep \
  && rm -rf /var/lib/apt/lists/*
RUN npm install -g --ignore-scripts @earendil-works/pi-coding-agent

WORKDIR /workspace
ENTRYPOINT ["pi"]
```

Build and run:

```bash
docker build -t pi-sandbox -f Dockerfile.pi .

docker run --rm -it \
  -e ANTHROPIC_API_KEY \
  -v "$PWD:/workspace" \
  -v pi-agent-home:/root/.pi/agent \
  pi-sandbox
```

The `-v "$PWD:/workspace"` mounts your current directory into the container at /workspace such that reads and writes in `/workspace` inside Docker directly affect your host files, like in the Gondolin example.

Use a named volume for `/root/.pi/agent` if you want container-local settings and sessions. Mounting your host `~/.pi/agent` exposes host auth and session files to the container.

## OpenShell

Use [NVIDIA OpenShell](https://docs.nvidia.com/openshell/about/overview) when you want a policy-controlled sandbox with filesystem, process, network, credential, and inference controls.
OpenShell can run sandboxes through a local gateway backed by Docker, Podman, or a VM runtime, or through a remote Kubernetes gateway.

Every sandbox requires an active gateway.
Register and select one before creating a sandbox:

```bash
openshell gateway add <gateway-url> --name <name>
openshell gateway select <name>
```

Launch `pi` inside an OpenShell sandbox:

```bash
openshell sandbox create --name pi-sandbox --from pi -- pi
```

In this pattern, the whole `pi` process runs inside the sandbox.
Built-in tools, `!` commands, and extension tools execute inside the OpenShell boundary.

If the gateway is remote, project files are not bind-mounted from the host, meaning writes in the sandbox are not reflected on your machine.
Clone the repository inside the sandbox or use OpenShell file transfer commands:

```bash
openshell sandbox upload pi-sandbox ./repo /workspace
openshell sandbox download pi-sandbox /workspace/repo ./repo-out
```

OpenShell providers can keep raw model API keys outside the sandbox.
When inference routing is configured, code inside the sandbox can call `https://inference.local`, and the gateway injects the configured provider credentials upstream.
Configure Pi to use the corresponding OpenAI-compatible or Anthropic-compatible endpoint if you want model traffic to use this route.

````
